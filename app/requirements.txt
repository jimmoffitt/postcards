# Deps for the ongoing app: curate.py (curation web tool) and poster.py
# (multi-worker posting daemon). Both run from this app/ directory.

Flask>=3.0.0
Pillow>=10.0.0
atproto>=0.0.46
python-dotenv>=1.0.0
